package exemplo3.entities;

public class SavingsAccountPlus extends SavingsAccount {
    // Não pode sobrescrever pois o metodo é final
    /*
    @Override
    public void withdraw(double amount) {
        if (amount <= balance) {
            balance -= amount + 3.0;
        }
    }
     */
}
